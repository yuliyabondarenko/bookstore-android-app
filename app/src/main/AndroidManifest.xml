<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jubee.bookstore">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.BookstoreApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activity.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.books.details.mvvm.BookDetailsMvvmActivity" />
        <activity android:name=".ui.activity.books.details.mvp.BookDetailsMvpActivity" />
        <activity android:name=".ui.fragment.books.list.mvvm.BookListMvvmFragment" />
        <activity android:name=".ui.fragment.books.list.mvp.BookListMvpFragment" />
    </application>

</manifest>